> Definí una función `burlarse` que reciba como argumento un string `str` y devuelva el mismo string con todas las vocales reemplazadas por la letra i
>
```javascript
ム burlarse('programar es dificil')
'prigimir is dificil'
```